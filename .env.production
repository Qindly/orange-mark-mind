# ============================================================================
# Orange Mark Mind - 生产环境变量配置
# 复制此文件到服务器，修改敏感信息后重命名为 .env
# ============================================================================

# --------------------------------------------------------------------------
# PostgreSQL 配置
# --------------------------------------------------------------------------
POSTGRES_DB=orange_mark_mind
POSTGRES_USER=omm_user
# 生产环境请使用强密码！
POSTGRES_PASSWORD=your_strong_postgres_password_here

# --------------------------------------------------------------------------
# Redis 配置
# --------------------------------------------------------------------------
# 生产环境请使用强密码！
REDIS_PASSWORD=your_strong_redis_password_here

# --------------------------------------------------------------------------
# JWT 配置
# --------------------------------------------------------------------------
# 生产环境请使用强随机字符串！可使用: openssl rand -base64 32
JWT_SECRET=your_strong_jwt_secret_key_here
JWT_ACCESS_TOKEN_EXPIRE=900
JWT_REFRESH_TOKEN_EXPIRE=604800

# --------------------------------------------------------------------------
# 加密配置
# --------------------------------------------------------------------------
# AES-256-GCM 加密密钥（必须是 32 字节）
# 可使用: openssl rand -base64 32 | head -c 32
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# --------------------------------------------------------------------------
# 服务配置
# --------------------------------------------------------------------------
GIN_MODE=release
